/node_modules
/dist

# secrets -- don't commit
/resources/credentials

# no project files
.vscode
.idea

# don't commit secrets (though this file is actually safe)
/.env

# winston logging files
/logs

# weird empty logs
.node**
